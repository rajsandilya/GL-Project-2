version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - pip install -r requirements.txt -t .
      - aws cloudformation package --template-file search-gateway.yaml --s3-bucket artifacts-bucket-gl --output-template-file search.yaml

artifacts:
  type: zip
  files:
    - search-gateway.yaml
    - search.yaml